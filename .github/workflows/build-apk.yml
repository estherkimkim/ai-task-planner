name: Build Android APK v5

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: ''

      - name: Install Android SDK packages
        run: |
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Unpack Android project v5
        run: |
          rm -rf appsrc
          mkdir appsrc
          unzip -q "android-project-v5.zip" -d appsrc

      - name: Verify v5 source
        run: |
          grep -q 'versionCode 5' appsrc/app/build.gradle
          grep -q 'public String geminiGenerate' appsrc/app/src/main/java/com/aitaskplanner/app/MainActivity.java
          grep -q '빌드 Android v5 · DB/Gemini 통합 수정판' appsrc/app/src/main/assets/index.html

      - name: Keep signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: ai-task-planner-debug-key-v2

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build APK
        working-directory: appsrc
        run: gradle --no-daemon assembleDebug

      - name: Verify APK
        run: |
          test -f appsrc/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-Task-Planner-v5-APK
          path: appsrc/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
